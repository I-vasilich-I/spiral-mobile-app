import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const GivingsScreen = (): JSX.Element => (
	<View>
		<Text>This is Givings screen</Text>
	</View>
);

export default GivingsScreen;
