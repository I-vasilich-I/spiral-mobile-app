import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		marginTop: 10,
		alignItems: 'center',
	},
	image: {
		width: 50,
		height: 50,
	},
	title: {
		color: 'grey',
		fontSize: 12,
	},
});

export default styles;
