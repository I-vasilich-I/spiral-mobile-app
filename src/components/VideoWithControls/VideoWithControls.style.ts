import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	image: {
		height: 200,
	},
	mute: {
		position: 'absolute',
		bottom: 15,
		right: 15,
	},
});

export default styles;
